import { TransactionBuilder } from "@orca-so/common-sdk";
import { Address } from "@project-serum/anchor";
import { PublicKey } from "@solana/web3.js";
import { WhirlpoolContext } from "..";
import { Position } from "../whirlpool-client";
export declare function collectForAllPositionAddrs(ctx: WhirlpoolContext, positionAddrs: Address[], refresh?: boolean): Promise<TransactionBuilder[]>;
export declare function collectForAllPositions(ctx: WhirlpoolContext, positions: Position[], destinationWallet?: PublicKey, positionOwner?: PublicKey, positionAuthority?: PublicKey, payer?: PublicKey): Promise<TransactionBuilder[]>;
export declare function isNativeMint(mint: PublicKey): boolean;
