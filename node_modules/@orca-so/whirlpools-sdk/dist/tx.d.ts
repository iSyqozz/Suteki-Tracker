import { WhirlpoolContext } from ".";
import * as ix from "./instructions/composites";
export declare class WhirlpoolTx {
    /**
     * A set of transactions to collect all fees and rewards from a list of positions.
     *
     * @param ctx - WhirlpoolContext object for the current environment.
     * @param params - CollectAllPositionAddressParams object.
     * @param refresh - if true, will always fetch for the latest values on chain to compute.
     * @returns
     */
    static collectAllForPositionsTx(ctx: WhirlpoolContext, params: ix.CollectAllPositionAddressParams, refresh: boolean): Promise<import("@orca-so/common-sdk").TransactionBuilder[]>;
}
